{
  "name": "ggs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon backend/server.js",
    "serve": "node backend/server.js",
    "build": "npm install && cd frontend && npm install && npm run build",
    "migrate:latest": "cd backend/db && knex migrate:latest",
    "migrate:down": "cd backend/db && knex migrate:down",
    "seed": "cd backend/db && knex seed:run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.3.0",
    "node-fetch": "^3.2.10",
    "nodemon": "^2.0.20",
    "path": "^0.12.7",
    "pg": "^8.8.0",
    "postgres": "^3.3.1"
  }
}
